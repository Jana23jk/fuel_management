const express = require('express');
const router = express.Router();

// Placeholder route
router.get('/', (req, res) => {
  res.send('Distance tracking route is under construction.');
});

module.exports = router;
